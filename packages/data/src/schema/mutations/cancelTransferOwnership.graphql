extend type Mutation {
  cancelTransferOwnership(
    input: CancelTransferOwnershipInput!
  ): CancelTransferOwnershipPayload
}

type CancelTransferOwnershipPayload {
  profile: Profile
}

input CancelTransferOwnershipInput {
  webCardId: ID!
}

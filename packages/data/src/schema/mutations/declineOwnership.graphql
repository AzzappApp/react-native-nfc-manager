extend type Mutation {
  declineOwnership(input: DeclineOwnershipInput!): DeclineOwnershipPayload!
}

input DeclineOwnershipInput {
  """
  profile id
  """
  profileId: ID!
}

type DeclineOwnershipPayload {
  profile: Profile!
}

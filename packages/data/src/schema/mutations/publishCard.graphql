extend type Mutation {
  """
  Update the card cover of the current profile
  """
  publishCard: PublishCardPayload!

  unpublishCard: PublishCardPayload!
}

type PublishCardPayload {
  webCard: WebCard!
}
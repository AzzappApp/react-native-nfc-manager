extend type Mutation {
  """
  Toggle like on a post
  """
  togglePostReaction(webCardId: ID!, input: TogglePostReactionInput!): TogglePostReactionPayload!
}

input TogglePostReactionInput {
  """
  The post id we want toggle like on
  """
  postId: ID!
  """
  The reaction kind we want to toggle
  """
  reactionKind: ReactionKind!
}

type TogglePostReactionPayload {
  post: Post!
}

extend type Mutation {
  removeUsersFromWebCard(
     webCardId: ID!, removedProfileIds: [ID!]!
  ): RemoveUserFromWebCardPayload!
}


type RemoveUserFromWebCardPayload {
  """
  the id of the profile that was removed from the web card
  """
  profileIds: [ID!]!
}

input SaveSocialLinksModuleInput {
  moduleId: ID
  links: [SocialLinkInput!]
  iconColor: String
  arrangement: CardModuleSocialLinksArrangement
  iconSize: Int
  borderWidth: Int
  columnGap: Int
  marginTop: Int
  marginBottom: Int
  backgroundId: ID
  backgroundStyle: ModuleBackgroundInputStyle
  clientMutationId: String
}

type SaveSocialLinksModulePayload {
  card: Card!
  clientMutationId: String
}

input SocialLinkInput {
  socialId: String!
  link: String!
  position: Int!
}

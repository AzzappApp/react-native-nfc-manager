extend type Mutation {
  filterWebCardSuggestion(
    input: FilterWebCardSuggestionInput!
  ): FilterWebCardSuggestionPayload!
}

input FilterWebCardSuggestionInput {
  webCardId: ID!
}

type FilterWebCardSuggestionPayload {
  webCard: WebCard!
}

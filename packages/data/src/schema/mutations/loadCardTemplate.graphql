extend type Mutation {
  """
  Remove a follower from the current user
  """
  loadCardTemplate(webCardId: ID!,  cardTemplateId: ID!): LoadCardTemplatePayload!
}


type LoadCardTemplatePayload {
  webCard: WebCard!
}

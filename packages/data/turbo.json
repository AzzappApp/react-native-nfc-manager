{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "typecheck": {
      "dependsOn": ["^build"],
      "inputs": ["src/**"]
    },
    "test": {
      "inputs": ["src/**", "jest.config.js", "scripts/jestSetup.js"]
    }
  }
}

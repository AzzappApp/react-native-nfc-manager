{
  "name": "@azzapp/service",
  "version": "1.9.0-canary.145",
  "private": true,
  "exports": {
    "./*": {
      "types": "./lib/*.d.ts",
      "default": "./lib/*.js"
    }
  },
  "scripts": {
    "lint": "eslint .",
    "test": "jest",
    "dev": "tsc --watch",
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf lib && rm -f tsconfig.build.tsbuildinfo && rm -rf node_modules"
  },
  "devScripts": [
    "dev"
  ],
  "dependencies": {
    "@azzapp/data": "workspace:^",
    "@azzapp/i18n": "workspace:^",
    "@azzapp/shared": "workspace:^"
  },
  "devDependencies": {
    "eslint": "^9.17.0",
    "jest": "^29.7.0",
    "typescript": "^5.8.2"
  }
}

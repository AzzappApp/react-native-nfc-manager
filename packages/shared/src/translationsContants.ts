/*
 * This prefix allows to query a translation on backend based $PREFIX$ID
 * "standard" translations automatically use id as a key
 * This prefix is used in multiple place and allow to query a second translation
 */
export const TEMPLATE_COVERTAG_DESCRIPTION_PREFIX = 'description-';

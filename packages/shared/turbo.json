{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "pipeline": {
    "build": {
      "inputs": ["src/**/*"],
      "outputs": ["lib/**/*"]
    },
    "typecheck": {
      "inputs": ["src/**/*"]
    }
  }
}

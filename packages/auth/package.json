{
  "name": "@azzapp/auth",
  "version": "0.1.3-canary.8",
  "private": true,
  "exports": {
    "./*": {
      "types": "./lib/*.d.ts",
      "default": "./lib/*.js"
    }
  },
  "scripts": {
    "dev": "tsc --watch",
    "build": "tsc  -p tsconfig.lib.json",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf lib && rm -f tsconfig.tsbuildinfo"
  },
  "devScripts": [
    "dev"
  ],
  "dependencies": {
    "@azzapp/shared": "0.1.3-canary.8"
  }
}

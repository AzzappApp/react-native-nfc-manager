{
  "name": "@azzapp/auth",
  "version": "0.0.4",
  "private": true,
  "exports": {
    "./*": {
      "types": "./lib/*.d.ts",
      "default": "./lib/*.js"
    }
  },
  "scripts": {
    "dev": "tsc --watch",
    "build": "tsc",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf lib && rm -f tsconfig.tsbuildinfo"
  },
  "devScripts": [
    "dev"
  ],
  "dependencies": {
    "@azzapp/shared": "*",
    "iron-webcrypto": "^0.6.0"
  }
}

extend type Mutation {
  removeContactsFromWebCard(
    profileId: ID!
    input: removeContactsFromWebCardInput!
  ): removeContactsFromWebCardPayload
  @deprecated(
    reason: "No longer used. Use removeContacts instead."
  )
}

input removeContactsFromWebCardInput {
  contactIds: [ID!]!
}

type removeContactsFromWebCardPayload {
  removedContactIds: [ID!]!
}

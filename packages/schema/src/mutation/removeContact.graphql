extend type Mutation {
  removeContact(
    profileId: ID!
    input: RemoveContactInput!
  ): RemoveContactPayload
}

input RemoveContactInput {
  profileId: String!
}

type RemoveContactPayload {
  removedContactId: ID!
}

extend type Mutation {
  createWebCard(input: CreateWebCardInput!): CreateWebCardPayload!
}

input CreateWebCardInput {
  userName: String!
  firstName: String
  lastName: String
  companyName: String
  webCardCategoryId: ID!
  companyActivityId: ID
}

type CreateWebCardPayload {
  profile: Profile!
}

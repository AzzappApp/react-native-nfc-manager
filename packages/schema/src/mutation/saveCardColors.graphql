extend type Mutation {
  """
  Update the card cover of the current profile
  """
  saveCardColors(webCardId: ID!, input: CardColorsInput!): SaveCardColorsPayload!
}

type SaveCardColorsPayload {
  webCard: WebCard!
}

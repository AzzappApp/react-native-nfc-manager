{
  "$schema": "https://turbo.build/schema.json",
  "extends": ["//"],
  "tasks": {
    "build": {
      "dependsOn": ["^build"],
      "inputs": ["src/**", "public/**"],
      "outputs": [".next/**"]
    },
    "deploy": {
      "dependsOn": ["^build"],
      "inputs": ["src/**", "public/**"],
      "outputs": []
    },
    "typecheck": { "dependsOn": ["^build"] },
    "test": { "inputs": ["src/**"] }
  }
}

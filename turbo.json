{
  "$schema": "https://turbo.build/schema.json",
  "globalDependencies": ["patches/*.patch"],
  "tasks": {
    "clean": { "dependsOn": [] },
    "build": { "dependsOn": [] },
    "dev": { "persistent": true, "cache": false, "dependsOn": [] },
    "//#relay:build": {
      "dependsOn": ["relay:prepare", "graphql-codegen"],
      "inputs": ["packages/app/*.{js,jsx,ts,tsx}", "./schema.graphql"],
      "outputs": [
        "packages/app/src/relayArtifacts/**",
        "packages/api/src/persisted-query-map.json"
      ]
    },
    "app:build": { "dependsOn": ["build", "//#relay:build"] },
    "//#relay:prepare": {
      "dependsOn": [],
      "inputs": [],
      "outputs": ["packages/api/src/persisted-query-map.json"]
    },
    "//#graphql-codegen": {
      "dependsOn": [],
      "inputs": ["packages/**/*.graphql"],
      "outputs": ["./schema.graphql"]
    },
    "lint": {}
  }
}
